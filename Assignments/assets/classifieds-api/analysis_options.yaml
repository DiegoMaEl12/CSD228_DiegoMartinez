analyzer:
  language:
    strict-inference: true
    strict-raw-types: true
    strict-casts: false
  # Since this is all generated we can ignore every file.
  exclude:
    - test/**/*.dart
    - lib/**/*.dart
  errors:
    deprecated_member_use_from_same_package: ignore
    unused_import: info
